pipeline {
    agent any
    stages {
        stage("init") {
            steps {
                echo "initializing..."
            }
        }
        
        stage("build image") {
            steps {
                echo "build image"
            }
        }

        stage("echo") {
            steps {
                echo "This is the end"
            }
        }
    }
}
